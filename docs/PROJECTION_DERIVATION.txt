======================================================================
WHAT EACH CONSUMER READS   (from its own source, not from a list)
======================================================================

eligibility(listing)  (4)
----------------------------------------------------------------------
    mileage_status
    price_kind
    price_status
    product_class

features_from_listing(listing)  (2)
----------------------------------------------------------------------
    body_condition
    document_issue

rows_from_corpus -> Row(...)  (7)
----------------------------------------------------------------------
    asking_price_toman
    listing_id
    make
    mileage_km
    model
    trim
    year_jalali

listing_from_record(rec) — corpus keys  (22)
----------------------------------------------------------------------
    asking_price_toman
    color
    condition
    condition_source
    document_issue
    fuel
    gearbox
    make
    mileage_km
    mileage_status
    model
    price_currency_raw
    price_kind
    price_kind_source
    price_status
    product_class
    product_class_source
    province
    seller_type
    source_url
    trim
    year_jalali

======================================================================
AGAINST FetchOutcome (28 fields)
======================================================================

A  the CORPUS chain needs  (23)
----------------------------------------------------------------------
    asking_price_toman
    body_condition
    color
    condition_source
    document_issue
    fuel
    gearbox
    listing_id
    make
    mileage_km
    mileage_status
    model
    price_currency_raw
    price_kind
    price_kind_source
    price_status
    product_class
    product_class_source
    province
    seller_type
    source_url
    trim
    year_jalali

B  REPOST matching needs  (10)   ← this is what last_signals is for
----------------------------------------------------------------------
    asking_price_toman
    color
    image_phashes
    make
    mileage_km
    model
    province
    seller_fingerprint
    trim
    year_jalali

C  needed by neither  (3)
----------------------------------------------------------------------
    http_status
    payload_sha
    status

  A ∩ B    8   both
  A \ B   15   corpus only
  B \ A    2   repost only
  A ∪ B   25   of 28 — a projection would drop 3

  The two slots do not want the same fields. One stored type
  serving both is one contract standing in for two.

======================================================================
READS NOTHING CAN SUPPLY
======================================================================

  none — every key a consumer reads is backed by a FetchOutcome field.
